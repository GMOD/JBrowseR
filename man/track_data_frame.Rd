% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_frame.R
\name{track_data_frame}
\alias{track_data_frame}
\title{Build a track from an R data frame}
\usage{
track_data_frame(data, name, assembly_name = NULL, ...)
}
\arguments{
\item{data}{A data frame with columns \code{chrom}, \code{start}, \code{end}, \code{name}. An
optional \code{score} column makes it a quantitative track; any \code{additional}
column is attached to each feature.}

\item{name}{Track display name.}

\item{assembly_name}{Assembly the track belongs to. Usually left \code{NULL} —
\code{\link[=JBrowseR]{JBrowseR()}} backfills it from the loaded assembly.}

\item{...}{Extra config merged into the track.}
}
\value{
a track config list
}
\description{
Turns a data frame of features into an in-browser track (a
\code{FromConfigAdapter}), no files or web server required. This is the natural
way to view results you computed in R — peaks, windows, hits — directly on
the genome.
}
\examples{
df <- data.frame(
  chrom = c("1", "2"),
  start = c(123, 456),
  end = c(789, 101112),
  name = c("feature1", "feature2")
)
track_data_frame(df, "my_features")
}

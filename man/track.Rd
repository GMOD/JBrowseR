% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tracks.R
\name{track}
\alias{track}
\title{Describe a data track from its URL}
\usage{
track(
  url,
  name = NULL,
  type = NULL,
  adapter_type = NULL,
  track_id = NULL,
  assembly_names = NULL,
  index = NULL,
  ...
)
}
\arguments{
\item{url}{URL to the track data.}

\item{name}{Track display name. Defaults to the file's base name.}

\item{type}{Override the inferred track type (e.g. \code{"AlignmentsTrack"}).}

\item{adapter_type}{Override the inferred adapter type.}

\item{track_id}{A unique id for the track. Defaults to \code{name}.}

\item{assembly_names}{Assembly name(s) the track belongs to. Usually left
\code{NULL} — \code{\link[=JBrowseR]{JBrowseR()}} backfills it from the loaded assembly.}

\item{index}{URL of the index file when it isn't the conventional sibling of
\code{url} (\code{.bai}/\code{.crai}/\code{.tbi}). A \code{.csi} index is detected by extension.}

\item{...}{Extra config merged into the track (e.g. \code{category = list("Genes")}).}
}
\value{
a track config list
}
\description{
Builds a track config, inferring the track type and adapter from the file
extension. JBrowse derives index locations (\code{.bai}/\code{.crai}/\code{.tbi}) and, for
CRAM, the reference sequence adapter from the assembly, so only the data URL
is required.
}
\details{
Recognized extensions: \code{.bam}/\code{.cram} (alignments), \code{.vcf.gz} (variants),
\code{.gff}/\code{.gff3}/\code{.gtf}/\code{.bed} gzipped (features), \code{.bb}/\code{.bigBed} (features),
\code{.bw}/\code{.bigWig} (quantitative), \code{.hic} (Hi-C contact matrix).
}
\examples{
track("https://jbrowse.org/genomes/hg19/gencode.v19.sorted.gff.gz", name = "Genes")
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tracks.R
\name{track}
\alias{track}
\title{Describe a data track by its URL}
\usage{
track(
  url,
  name = NULL,
  track_id = NULL,
  assembly_names = NULL,
  index = NULL,
  ...
)
}
\arguments{
\item{url}{URL to the track data.}

\item{name}{Track display name. Left \code{NULL}, the view derives it from the
file's base name.}

\item{track_id}{A unique id for the track. Left \code{NULL}, the view derives one.}

\item{assembly_names}{Assembly name(s) the track belongs to. Usually left
\code{NULL} — \code{\link[=JBrowseR]{JBrowseR()}} backfills it from the loaded assembly.}

\item{index}{URL of the index file when it isn't the conventional sibling of
\code{url} (\code{.bai}/\code{.crai}/\code{.tbi}). A \code{.csi} index is detected by extension.}

\item{...}{Extra config merged onto the track, overriding the inferred
defaults (e.g. \code{category = list("Genes")}, or a \code{type = "AlignmentsTrack"}
override).}
}
\value{
a loose track spec list
}
\description{
Builds a loose track spec — \code{list(uri = url)} plus whatever you pass — that
the view expands into a full track config when it loads, inferring the track
type and adapter from the file extension with JBrowse's own format plugins
(the same inference the "Add track" flow uses). No extension table lives in
R, so every format a bundled plugin recognizes works: \code{.bam}/\code{.cram}
(alignments), \code{.vcf} (variants), \code{.gff}/\code{.gff3}/\code{.gtf}/\code{.bed} (features),
\code{.bb}/\code{.bigBed} (features), \code{.bw}/\code{.bigWig} (quantitative), \code{.hic} (Hi-C), and
more. A bgzipped file (\code{.gff.gz}, …) resolves to its indexed tabix adapter, a
plain one to the whole-file adapter. JBrowse derives index locations
(\code{.bai}/\code{.crai}/\code{.tbi}) and, for CRAM, the reference from the assembly, so
only the data URL is required.
}
\examples{
track("https://jbrowse.org/genomes/hg19/gencode.v19.sorted.gff.gz", name = "Genes")
}

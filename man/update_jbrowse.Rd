% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/update.R
\name{update_jbrowse}
\alias{update_jbrowse}
\title{Change options of a rendered browser}
\usage{
update_jbrowse(outputId, ..., domain = shiny_domain())
}
\arguments{
\item{outputId}{The browser's output id. Inside a Shiny module, pass the bare
id, as you would to \verb{output$}.}

\item{...}{Options to change, each named.}

\item{domain}{The Shiny session, defaulting to the current one. Not named
\code{session}, which is an option.}
}
\value{
\code{outputId}, invisibly.
}
\description{
Sends option changes to a \code{\link[=JBrowseR]{JBrowseR()}} or \code{\link[=JBrowseRApp]{JBrowseRApp()}} already on the
page, without re-running its render expression. The changes are named the
way the widget's own options are.
}
\details{
\if{html}{\out{<div class="sourceCode r">}}\preformatted{observeEvent(input$gene, update_jbrowse("browser", location = input$gene))
}\if{html}{\out{</div>}}

A \code{\link[=JBrowseR]{JBrowseR()}} applies \code{tracks} and \code{location} in place and a \code{\link[=JBrowseRApp]{JBrowseRApp()}}
applies \code{session} in place; any other option builds a new browser from the
rendered options plus the changes. A later re-render compares against what
it rendered last, so a change sent here survives a re-render that does not
restate that option.
}
